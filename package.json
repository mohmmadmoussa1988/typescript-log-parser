{
  "name": "nodejs-logger-qwzclm",
  "version": "1.0.0",
  "description": "Develop the command line node.js application, which parses the input log file (see Input Example). The application should find all the log messages with the level error and print them into the output file (see Output Format).",
  "main": "parser.js",
  "scripts": {
    "test": "jest",
    "watch": "node_modules/.bin/nodemon",
    "build": "npx tsc",
    "exportErrors": "node ./parser.js --input app.log --output erros.json",
    "help": "node ./parser.js --help"
  },
  "repository": {
    "type": "git",
    "url": "http://berbob-gmbh-ftcbiq@git.codesubmit.io/berbob-gmbh/nodejs-logger-qwzclm"
  },
  "keywords": [
    "nodejs",
    "log",
    "parser"
  ],
  "author": "Mohammad Moussa",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^29.5.2",
    "@types/mock-fs": "^4.13.1",
    "@types/node": "^20.2.5",
    "jest": "^29.5.0",
    "nodemon": "^2.0.22",
    "ts-jest": "^29.1.0",
    "typescript": "^5.1.3"
  },
  "dependencies": {
    "commander": "^10.0.1",
    "mock-fs": "^5.2.0",
    "winston": "^3.9.0"
  }
}
